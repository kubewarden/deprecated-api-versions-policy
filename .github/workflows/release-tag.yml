on:
  pull_request:
    types:
      - closed
  workflow_dispatch:
    inputs:
      trigger_release:
        description: "Tag and trigger release manually"
        required: false
        default: true

name: Tag and Release on PR Merge

jobs:
  test:
    name: release-tag
    uses: kubewarden/github-actions/.github/workflows/reusable-release-tag.yml@17f4dbf90d74f26892f7cc18179d08c4d63914d9 # v4.5.4
