on:
  pull_request:
    types:
      - closed
  workflow_dispatch:
    inputs:
      trigger_release:
        description: "Tag and trigger release manually"
        required: false
        default: true

name: Tag and Release on PR Merge

jobs:
  test:
    name: release-tag
    uses: kubewarden/github-actions/.github/workflows/reusable-release-tag.yml@847a8b8af7715a709c56426d3d298a90d7a6dad3 # v4.5.13
